# Copiá a .env para desarrollo local de la API standalone (no commitear .env)

PORT=3847
HOST=0.0.0.0
BODEGA_DATA_DIR=./data-api
JWT_SECRET=cambiá-esto-en-railway

# Slice 2 — Postgres (Railway lo inyecta al linkear el plugin Postgres)
# Si está definido, la API usa Postgres y no SQLite.
# DATABASE_URL=postgresql://user:pass@host:5432/railway
