/** Estilos compartidos para ítems resaltados al navegar con flechas. */

/** Fila en dropdown / lista compacta (buscador de productos). */
export const KB_HIGHLIGHT_ROW =
  'bg-brand-100 text-brand-950 ring-2 ring-inset ring-brand-400 shadow-sm'

/** Fila en listados de registros (ingresos, retornos, etc.). */
export const KB_HIGHLIGHT_LIST_ROW =
  'bg-brand-100 ring-2 ring-inset ring-brand-400 shadow-sm'

/** Tarjeta expandible resaltada (consulta, inventario). */
export const KB_HIGHLIGHT_CARD =
  'border-brand-400 bg-brand-50 shadow-md ring-2 ring-brand-400/70'

/** Fondo de la fila interna de una tarjeta resaltada. */
export const KB_HIGHLIGHT_CARD_INNER = 'bg-brand-100/90'

/** Botón / celda compacta (panel sector, etc.). */
export const KB_HIGHLIGHT_COMPACT =
  'bg-brand-100 ring-2 ring-inset ring-brand-400 shadow-sm'

/** Módulo grande con anillo externo (dashboard, reportes). */
export const KB_HIGHLIGHT_MODULE =
  'ring-2 ring-brand-600 ring-offset-2 ring-offset-white z-[1]'

/** Registro recién confirmado: destello corto en el listado. */
export const REGISTRO_NUEVO_FLASH = 'registro-nuevo-flash'
